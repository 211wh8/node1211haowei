var fs = require("fs");
function copy(a,b) {
    var rs = fs.createReadStream(a);
    var ws = fs.createWriteStream(b);
    // var data = fs.readFileSync(a,"utf-8");
    // console.log(data);
    rs.pipe(ws);
    // fs.writeFileSync(b,data,{flag:"w"})
}

module.exports={
    copy:copy
}